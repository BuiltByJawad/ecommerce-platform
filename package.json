{
  "name": "ecommerce-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck -w frontend || true",
    "test": "npm run test -w backend && npm run test -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "ci": "npm ci --ignore-scripts && npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
